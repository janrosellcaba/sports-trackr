DATABASE_URL="file:./dev.db"
AUTH_SECRET="change-me-in-production-min-16"
REGISTRATION_CODE="change-me"
# Comma-separated extra hosts for `next dev` on a phone, if LAN detection misses one:
# ALLOWED_DEV_ORIGINS="192.168.1.20"

# Contact form (Resend). Same keys as my-finance. Leave RESEND_API_KEY empty to disable sending.
RESEND_API_KEY=
CONTACT_EMAIL=jan@janrosell.com
RESEND_FROM=onboarding@resend.dev
